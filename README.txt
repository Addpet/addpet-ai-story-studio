ADDPET STORY STUDIO — AI STORY GENERATOR

Deploy the project to Netlify.
Then add an environment variable:
OPENAI_API_KEY = your OpenAI API key

Never put the API key inside index.html or share it publicly.
After adding the variable, redeploy the site.
